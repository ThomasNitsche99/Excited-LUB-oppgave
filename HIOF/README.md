# HIOf Learning Outcomes Scraping

This script scrapes learning outcomes from the HIOf data engineering bachelor's program webpage and saves them to a local `.txt` file.

## Usage

Run `hiof_scraping_script.py` to produce a `.txt` file with the learning outcomes for the HIOf program.

## Requirements

Install the requirements using:

```bash
pip install -r requirements.txt
